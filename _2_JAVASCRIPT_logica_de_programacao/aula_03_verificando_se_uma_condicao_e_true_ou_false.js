const maior_idade = 18;
let nome = 'Gustavo';
let idade = 17;
let podeEntrar = false;


if (idade >= maior_idade || nome === 'Gusta vo') {
    podeEntrar = true;
    console.log('Usuário pode entrar', podeEntrar);
} else {
    console.log('Usuário não pode entrar', podeEntrar);
}